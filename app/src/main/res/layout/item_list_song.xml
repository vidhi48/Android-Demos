<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="songData"
            type="com.example.demo.recyclerview.models.Song" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/songProfile"
            android:layout_width="@dimen/dp_60"
            android:layout_height="@dimen/dp_60"
            android:contentDescription="@string/app_name"
            android:src="@{songData.songProfile}"
            tools:src="@drawable/shape_song"
            android:scaleType="fitXY"
            android:layout_marginTop="@dimen/dp_25"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/songTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/dp_15"
            android:layout_marginTop="@dimen/dp_10"
            android:text="@{songData.songTitle}"
            android:textColor="@color/black"
            android:textSize="17sp"
            android:textStyle="bold"
            android:fontFamily="@font/dancing_script_variable_font"
            tools:text="@string/shape"
            app:layout_constraintStart_toEndOf="@id/songProfile"
            app:layout_constraintTop_toTopOf="@id/songProfile" />

        <TextView
            android:id="@+id/singer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{songData.singerName}"
            android:textColor="@color/gray"
            android:textSize="15sp"
            tools:text="@string/shapeSinger"
            app:layout_constraintStart_toStartOf="@id/songTitle"
            app:layout_constraintTop_toBottomOf="@id/songTitle" />

        <TextView
            android:id="@+id/time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{songData.time}"
            tools:text="@string/shapeTime"
            android:textColor="@color/black"
            android:textSize="15sp"
            android:layout_marginEnd="@dimen/dp_15"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/singer" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>